<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <ImageView
            android:id="@+id/imgImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_width="@dimen/button_pipup_image_parent"
            android:layout_height="@dimen/button_pipup_image_parent"
            android:layout_gravity="top|start"
            android:layout_margin="@dimen/margin_edt_msg"
            android:background="@drawable/ripple_image_popup"
            android:src="@drawable/ic_baseline_close_24" />

        <ImageButton
            android:id="@+id/btnMore"
            android:layout_width="@dimen/button_pipup_image_parent"
            android:layout_height="@dimen/button_pipup_image_parent"
            android:layout_gravity="top|end"
            android:layout_margin="@dimen/margin_edt_msg"
            android:background="@drawable/ripple_image_popup"
            android:src="@drawable/ic_baseline_more_image_popup_24" />

    </FrameLayout>
</layout>